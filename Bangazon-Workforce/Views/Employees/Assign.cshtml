@model Bangazon_Workforce.Models.ViewModels.AssignToProgramViewModel

@{
    ViewData["Title"] = "Assign";
}

<h1>Assign</h1>

<h4>Assign Employee to Training Program</h4>
<hr />
<dl>
    <dt class="col-sm-2">
        First Name
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.EmployeeFirstName)
    </dd>
    <dt class="col-sm-2">
        Last Name
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.EmployeeLastName)
    </dd>
    <dt class="col-sm-2">
        Training Programs
    </dt>
   

</dl>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Assign">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <select asp-for="TrainingProgramIds" asp-items="@Model.TrainingProgramOptions" class="form-control" multiple>
                </select>
                <span asp-validation-for="TrainingPrograms" class="text-danger"></span>
                </div>
                <input type="hidden" asp-for="EmployeeId" value="@Model.EmployeeId" />

                <div class="form-group">
                    <input type="submit" value="Assign" class="btn btn-primary" />
                </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
